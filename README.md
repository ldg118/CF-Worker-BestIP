# \**CF 优�?IP · 双池智能优�?*

基于 Cloudflare Workers �?IP 优选工具，支持带宽测试、延迟测试、智�?DNS 更新、多国家分流。感觉有帮助的可以留颗小✨✨�?
**当前版本**: v4.6.2

## 项目特点

| 核心功能                | 智能管理                 | 界面体验              |
| ------------------- | -------------------- | ----------------- |
| 📡 IP 自动发现与测�?      | 🎯 双池智能优选（带宽�?+ 备用池） | 🎨 卡片式布局，支持折叠展开   |
| �?访客位置感知            | 🌍 **多国�?DNS 智能分流**  | ⚙️ 可视化运行参数配�?     |
| 🌐 自动 DNS 更新        | 🧠 **智能分流更新**        | 📊 实时测速结果展�?      |
| 📥 IP 导出（优质池 + 备用池） | 🗑�?清理带宽�?& 黑名�?     | 🔍 IP 搜索功能        |
| 📱 Telegram 通知      | �?定时任务维护             | 📝 日志管理�?-7天自动清理） |
| 📌 自定�?IP 导入        | 🔄 智能速率限制            | 🔧 数据库状态监�?       |

## 界面预览

### 登录界面

!\[登录界面]\(docs/images/1.png)

简洁的登录页面，输入管理员密码即可进入系统�?

### 主界�?- 顶部导航

!\[主界面顶部]\(docs/images/2.png)

顶部显示访客位置感知信息，包括：

- 您的 IP 地址
- 您的地理位置
- 本地优质 IP 数量
- 本地平均延迟和带�?

### 主界�?- IP池管�?

!\[主界面IP池]\(docs/images/3.png)

三栏式卡片布局�?

- **左侧**：带宽优质池和备用池状�?- **中间**：快速操作按钮（DNS更新、导出、清理等�?- **右侧**：数据分析与配置日志区域

## 部署步骤

### 1. 准备工作

- Cloudflare 账号
- Workers KV 命名空间
- D1 数据�?

### 2. 部署�?Cloudflare Workers

1. 登录 Cloudflare 控制�?2. 进入 " Workers �?Pages "
2. 选择你的 Worker 或创建一个新�?Worker
3. 点击 "编辑代码"
4. 复制 `worker.js 或�?混淆加密` 文件的内�?6. 粘贴到代码编辑器�?7. 点击 "保存并部�?

### 3. 配置环境变量

�?Cloudflare Workers 控制台中，点�?"设置" �?"变量"，添加以下环境变量：

**必需配置**�?

| 环境变量             | 描述     | 示例�?  |
| ---------------- | ------ | ----- |
| `ADMIN_PASSWORD` | 管理员密�? | `123` |

\*\*可选配�?\*（不配置则使用默认值）�?

| 环境变量                          | 描述         | 默认�? |
| ----------------------------- | ---------- | ---- |
| `DEFAULT_IP_COUNT`            | 默认 IP 数量   | `3`  |
| `DEFAULT_TEST_COUNT`          | 默认测试数量     | `30` |
| `DEFAULT_THREAD_COUNT`        | 默认线程数量     | `10` |
| `DEFAULT_BANDWIDTH_FILE_SIZE` | 默认带宽测试文件大小 | `3`  |
| `FAILED_IP_COOLDOWN_DAYS`     | 失败 IP 冷却天数 | `15` |
| `MAX_HIGH_QUALITY_POOL_SIZE`  | 最大优质池大小    | `30` |

### 4. 配置数据�?

1. \*\*创建 D1 数据�?\*�?   - 进入 Cloudflare 控制台的 D1 部分
   - 创建一个新的数据库，名称为 `cf-best-ip-db`
2. \*\*配置数据库绑�?\*�?   - �?Workers 控制台中，点�?"设置" �?"绑定"
   - 点击 "添加绑定"
   - 选择 "D1 数据�?
   - 绑定名称：`DB`
   - 选择你创建的 D1 数据�?   - 点击 "保存"
3. \*\*初始化数据库�?\*�?   - �?D1 数据库控制台中，运行 `migrations/001_init.sql` 中的 SQL 语句

### 5. 配置 KV 存储

1. **创建 KV 命名空间**�?   - 进入 Cloudflare 控制台的 KV 部分
   - 创建一个新�?KV 命名空间，名称为 `cf-best-ip-kv`
2. **配置 KV 绑定**�?   - �?Workers 控制台中，点�?"设置" �?"绑定"
   - 点击 "添加绑定"
   - 选择 "KV 命名空间"
   - 绑定名称：`KV`
   - 选择你创建的 KV 命名空间
   - 点击 "保存"

### 6. 配置定时任务

1. \*\*配置 Cron 触发�?\*�?   - �?Workers 控制台中，点�?"设置" �?"触发�?
   - 点击 "添加 Cron 触发�?
   - 输入 cron 表达式：`0 * * * *`
   - 点击 "保存"

## 多国�?DNS 智能分流

系统支持为不同国�?地区配置不同�?DNS 域名，实现智�?IP 分流�?

### 配置方法

�?Web 界面 "DNS 设置" 中配置国家域名映射：

| 国家 | 域名列表                                    |
| -- | --------------------------------------- |
| CN | cf-cn.example.com, cf-china.example.com |
| US | cf-us.example.com                       |
| JP | cf-jp.example.com                       |

### 智能分流规则

- 系统根据 IP 的国家属性自动分�?- 每个国家�?IP 仅更新到对应国家的域�?- 支持一个国家配置多个域名（批量更新�?- 未匹配国家的 IP 默认更新到主域名

### 使用场景

- 为不同地区用户提供最�?IP
- 实现地理负载均衡
- 优化跨国访问速度

## 界面布局说明

系统采用全新的卡片式布局设计�?

### 布局结构

````
┌─────────────────────────────────────────────────────────────�?�? 顶部导航�?(统计信息 + 操作按钮)                              �?├─────────────────┬───────────────────┬───────────────────────�?�?                �?                  �?                      �?�? 优质 IP �?     �?  备用 IP �?      �?   操作面板           �?�? (可折�?        �?  (可折�?         �?   (可折�?           �?�?                �?                  �?                      �?�? �?IP 列表       �?  �?IP 列表        �? �?运行参数配置       �?�? �?导出 CSV     �?  �?导出 CSV      �? �?DNS 设置          �?�? �?统计信息     �?  �?统计信息       �? �?测速控�?         �?�?                �?                  �?                      �?├─────────────────┴───────────────────┴───────────────────────�?�? 底部功能�?(日志、黑名单管理�?                               �?└─────────────────────────────────────────────────────────────�?```

### 交互特�?
- **卡片独立折叠**：每个卡片可以独立展开/折叠
- **悬停效果**：鼠标悬停时显示操作按钮
- **响应式设�?*：自适应不同屏幕尺寸
- **深色主题**：护眼配色方�?
## 故障排除

### 常见错误

1. **DB is not defined**
   - 原因：D1 数据库绑定未配置
   - 解决：按照步�?4 配置 D1 数据库绑�?2. **API 响应 500**
   - 原因：内部错�?   - 解决：检�?Cloudflare Workers 日志，查看具体错误信�?3. **DNS 操作失败**
   - 原因：Cloudflare API 令牌或区�?ID 错误
   - 解决：在 Web 界面 "DNS 设置" 中检查配�?4. **Telegram 通知失败**
   - 原因：Telegram 机器人令牌或聊天 ID 错误
   - 解决：在 Web 界面 "DNS 设置" �?"Telegram 通知" 中检查配�?
### 日志查看

1. 登录 Cloudflare 控制�?2. 进入 " Workers �?Pages "
3. 选择你的 Worker
4. 点击 "Observability" �?"Logs"
5. 查看实时日志和错误信�?
## 性能优化

1. **缓存策略**：使�?KV 存储缓存频繁访问的数�?2. **批处�?*：批量处�?IP 测试，减�?API 调用
3. **错误处理**：优雅处理错误，避免系统崩溃
4. **资源限制**：遵�?Cloudflare Workers 的资源限�?
## 技术架�?
### 模块化结�?
````

src/
├── api.js               # API 路由模块
├── config.js            # 配置管理模块
├── database.js          # 数据库操作模�?├── dns.js               # DNS 管理模块
├── errorHandler.js      # 错误处理模块
├── notification.js      # 通知系统模块
├── utils.js             # 工具函数模块
└── worker.js            # 主入口文�?\`\`\`

### 核心功能

- **IP 测试**：测�?IP 地址的响应时间和可用�?- **DNS 管理**：管�?Cloudflare DNS 记录
- **通知系统**：发�?Telegram 通知
- **数据存储**：使�?D1 数据库存储测试结果和配置
- **缓存管理**：使�?KV 存储缓存数据
- **定时任务**：定期执�?IP 测试和其他维护任�?

## 安全注意事项

1. **API 令牌**：不要在代码中硬编码 API 令牌，使用环境变�?2. **密码**：使用强密码，并通过环境变量配置
2. **输入验证**：验证所有用户输入，防止注入攻击
3. **速率限制**：实现速率限制，防止滥�?API

## 许可�?

MIT License
